import AppBar from '../../components/GenericAppBar';
import { Strings } from '../../constants'

const FdrScreen = () => {
    return (
        <>
            <AppBar
                title={Strings.fdr}
            />

        </>
    );
};

export default FdrScreen;
